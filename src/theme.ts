import { createTheme } from "@mantine/core";

export const theme = createTheme({
  fontFamily: "Playfair",
  fontSizes: {
    xs: "16px",
    sm: "18px",
    md: "20px",
    lg: "24px",
    xl: "32px",
  },
  headings: {
    fontFamily: "Outfit"
  }
 
});
