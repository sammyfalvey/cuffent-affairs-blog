import { createTheme, em } from "@mantine/core";

export const theme = createTheme({
  fontFamily: "Playfair",
  fontSizes: {
    xs: "20px",
    sm: "22px",
    md: "24px",
    lg: "28px",
    xl: "32px",
  },
  headings: {
    fontFamily: "Outfit"
  }
 
});
